// To intialise npm

// npm init - asks for package name
// npm init -y just install the package.json file without asking for name

// To install a package we use

// npm install package-name
// npm i package-name

// To install a speific version
// npm install package-name@version